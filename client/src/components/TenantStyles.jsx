/**
 * Los estilos del tenant se aplican via TenantContext (CSS variables en el DOM).
 * Este componente no renderiza nada — se conserva por compatibilidad.
 */
export default function TenantStyles() {
  return null
}
